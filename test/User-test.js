import chai from 'chai';
const expect = chai.expect;

describe('User', () => {
  let user;
  
  beforeEach(() => {
   newUser = new User()
  });

    it('Should be a function', () => {
      expect(Recipe).to.be.a('function');
    });

  }
    