

class User {
  constructor(user) {
    this.id = user.id;
    this.name = user.name;
    this.roomAlreadyBooked = []; //need a method for this
  }
}
//method to get user rooms already books.
export default User;

